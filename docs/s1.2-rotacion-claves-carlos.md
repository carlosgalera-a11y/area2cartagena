# Rotación de claves — acciones pendientes de Carlos
Fecha: 2026-04-21 · Bloqueante único restante del TOP-4 de `docs/pendientes-financiacion-v1.0.0.md`

## Contexto

Tras `git filter-repo` (hecho hoy), la historia pública de los repos `Cartagenaeste` + `area2cartagena` ya no contiene ninguna de las claves detectadas. Pero **asumir comprometidas TODAS las que estuvieron en el repo en algún momento**: GitHub ya las indexó, sus Secret Scanning pudieron exponerlas a terceros, bots scrappers las pueden haber copiado.

**Regla**: una clave que ha tocado un repo público alguna vez → se rota, no se rescata.

---

## Las 3 claves que debes rotar en los próximos 30 min

### 1. DeepL (si sigue en uso)

- **Valor quemado**: `[REDACTED_DEEPL_2026-04-21]` (revocada — ver Secret Manager para la nueva)
- **Panel**: https://www.deepl.com/pro-account/summary
- **Acción**:
  1. Login.
  2. "Account" → "API Keys" → Copy current → regenerate.
  3. Nueva key al portapapeles.
- **Almacenar en Secret Manager** (si la Cloud Function askAi algún día usa DeepL):
  ```bash
  cd /Users/carlos/cartagenaestewebappSOLIDA
  firebase functions:secrets:set DEEPL_API_KEY
  # pega la nueva cuando pida input
  pbcopy < /dev/null   # vacía portapapeles después
  ```
- **Decisión pragmática**: hoy el código no llama a DeepL (ya refactorizamos todo a `askAi`). Si no la vas a usar → **simplemente revoca la vieja y no crees nueva**. Ahorro: 0 €/mes.

### 2. FH_BLOG_WP (WordPress de cartagenaeste.es)

- **Valor quemado**: `[REDACTED_FH_WP_2026-04-21]` (revocada en WP)
- **Panel**: admin WP de `cartagenaeste.es`
- **Acción**:
  1. Login en https://cartagenaeste.es/wp-admin
  2. Buscar plugin "FileHub API" → Settings → API Keys → Rotate.
  3. Copiar nueva key.
- **Actualización en app.html** (cambia el Blog Publisher de la pestaña FileHub):
  - Edita `app.html` línea ~5188: reemplaza el valor de `FH_BLOG_WP.apiKey` (actualmente `REDACTED_FH_WP_2026-04` tras el filter-repo) por la nueva clave.
  - O mejor: mueve la clave a Firestore en `config/fh_blog_wp` con rules que solo permitan lectura al propio Carlos, y lee de ahí en runtime.
- **Decisión pragmática**: si el Blog Publisher (`openBlogPublisher()`) no se usa activamente, revoca la vieja en WP y deja el FileHub desactivado. Menos superficie de ataque.

### 3. Groq (deprecada — solo revocar, no regenerar)

- **Valor quemado**: `[REDACTED_GROQ_KEY_1_2026-04-21]` y `[REDACTED_GROQ_KEY_2_2026-04-21]`
- **Panel**: https://console.groq.com/keys
- **Acción**:
  1. Login.
  2. Para CADA key listada → botón 🗑 (trash icon).
  3. Confirmar.
- **NO crear nueva**. El plan técnico (CLAUDE.md) no incluye Groq. Lo usaba `enfCallAI`, `scanIA`, `nihSendToGroqVision`, `fhSendMsg`, `fhBlogGeneratePost` — TODOS migrados a `askAi` (PR #19).

---

## Verificación final (cuando termines las 3 rotaciones)

```bash
cd /Users/carlos/cartagenaestewebappSOLIDA
firebase functions:secrets:access DEEPSEEK_API_KEY   2>&1 | head -c 12 ; echo "..."
firebase functions:secrets:access OPENROUTER_API_KEY 2>&1 | head -c 12 ; echo "..."
firebase functions:secrets:access DEEPL_API_KEY      2>&1 | head -c 12 ; echo "..."   # solo si decidiste crearla
```

Los 2 primeros deben devolver los primeros chars de la clave nueva. Groq y FH_BLOG_WP NO van en Secret Manager (Groq no se usa; FH_BLOG_WP vive en app.html si decides mantenerlo).

---

## Estado de los 4 bloqueantes del dossier

| # | Bloqueante | Estado | Quién |
|---|---|---|---|
| 1 | Rotar 3 claves (DeepL, FH_BLOG_WP, Groq) | ⏳ Pendiente (15–30 min GUI) | Carlos |
| 2 | git filter-repo histórico | ✅ Hecho (11 patrones purgados, 0 matches full) | Claude Code |
| 3 | Branch protection main (ambos repos) | ✅ Hecho via `gh api` (force_push=false, enforce_admins=true, PR required) | Claude Code |
| 4 | Verificar admin@cartagena.es en Firebase Auth | ✅ NO existe (54 users, ninguno es admin fantasma) | Claude Code |

---

## Backups locales (por si algo sale mal en las próximas 48h)

- `/tmp/filter-repo-backup-2026-04-21/Cartagenaeste-backup.git` (mirror, 496 MB)
- `/tmp/area2cartagena-backup-2026-04-21.git` (mirror, 417 MB)

Si necesitas restaurar:

```bash
cd ~
git clone --mirror /tmp/filter-repo-backup-2026-04-21/Cartagenaeste-backup.git rescue.git
cd rescue.git
git push --mirror https://github.com/carlosgalera-a11y/Cartagenaeste.git
# NOTA: requiere desactivar branch protection primero con gh api.
```

**Borrar los backups el 2026-05-21** (30 días):

```bash
rm -rf /tmp/filter-repo-backup-2026-04-21
rm -rf /tmp/area2cartagena-backup-2026-04-21.git
```

---

## Nota sobre la nueva historia git

Tras el filter-repo, **todos los commit hashes cambiaron**. Esto tiene consecuencias:

- La etiqueta `v1.0.0` ahora apunta al commit reescrito `876b71a` (antes `dc41c41`).
- Cualquier enlace externo a un commit concreto del repo (blog posts, dossiers, tickets) queda roto.
- Los GitHub issues que referenciaban commits por hash quedan huérfanos.
- Los PRs #1-#20 siguen existiendo en GitHub con su número, pero los diffs mostrados ahora corresponden a la historia reescrita.

No hay nada urgente que hacer al respecto — solo saberlo por si recibes una pregunta sobre "este link ya no funciona".
