# Deploy pendiente · Carlos · 2026-04-21 (actualizado)

Estado tras las 11 PRs mergeadas + sync al repo `area2cartagena`.

---

## ✅ Ya hecho

- PRs #1 a #8 mergeados a `Cartagenaeste/main`.
- `askAi` desplegada en Cloud Functions `europe-west1` (Node 22).
- Secretos `DEEPSEEK_API_KEY` + `OPENROUTER_API_KEY` cargados en Secret Manager.
- `App Check enforce` **desactivado** (`enforceAppCheck: false` en `askAi.ts`) — se podrá activar en PR futuro tras reCAPTCHA v3.
- Sync force-push de `Cartagenaeste/main` → `area2cartagena/main` ejecutado.
  - Backup local: `/tmp/area2cartagena-backup-2026-04-21.git`
  - Rama rescate en remoto: `area2cartagena/pre-sync-backup-2026-04-21`
- GitHub Pages rebuild verificado (status `built`) → `area2cartagena.es` sirve la nueva versión.
- `ADMIN-CREDENTIALS.md` ya devuelve HTTP 404 en `area2cartagena.es` (purgado).
- IP interna `192.168.1.35` → 0 matches en index.html público.
- `ai-client.js` disponible públicamente (HTTP 200).
- Groq completamente eliminado del stack IA tras decisión de Carlos.

---

## 🔴 Por hacer · Carlos (manual, GUI)

### 1. Rotar/revocar claves expuestas (15 min total)

Aunque el sync limpia el código servido, las claves que estuvieron en el HTML público siguen siendo válidas en los providers hasta que se revoquen.

| Clave | Estado | Dónde rotar |
|---|---|---|
| Groq `gsk_GTTqfTXpC5…` | Borrar (no se usa ya) | https://console.groq.com/keys → 🗑 |
| OpenRouter #1 `sk-or-v1-6f0e1c0c…` | Rotada tras PR #4 | ya hecha |
| OpenRouter #2 `sk-or-v1-9ccad5a7…` | Sigue válida si es distinta | https://openrouter.ai/keys → Delete |
| DeepSeek antigua `sk-6a5ea8df…` | Rotar si activa | https://platform.deepseek.com/api_keys |
| DeepSeek nueva `sk-a89dd17f…` | Rotar si activa | https://platform.deepseek.com/api_keys |
| DeepL `adaad50f-aa27-…` | Rotar | https://www.deepl.com/pro-account/summary |
| FH_BLOG_WP `fh_k8x2pL…` | Rotar o eliminar integración | Backend WP cartagenaeste.es |

### 2. Firestore cleanup (2 min)

Borrar el doc `config/groq_api_key` que guardaba una clave Groq antigua:
https://console.firebase.google.com/project/docenciacartagenaeste/firestore/data/~2Fconfig~2Fgroq_api_key

### 3. TTL policies en Firestore (2 min)

Sin estas, `aiCache` y `rate_limits_ip` crecen indefinidamente. Crear dos TTL policies en:
https://console.firebase.google.com/project/docenciacartagenaeste/firestore/ttl

| Collection ID    | Timestamp field |
|------------------|-----------------|
| `aiCache`        | `expiresAt`     |
| `rate_limits_ip` | `expiresAt`     |

### 4. Probar el chatbot (2 min)

Ya con la nueva versión servida:

1. Abre https://area2cartagena.es/chatbot-medicacion.html (`⌘+Shift+R` si ves versión vieja).
2. Verás banner amarillo "🔐 Para usar la IA, inicia sesión con tu cuenta de Google".
3. Pulsas "Iniciar sesión con Google" → popup → tu cuenta.
4. Pregunta algo tipo "ibuprofeno y omeprazol juntos".
5. Respuesta debe venir de `askAi` (texto variable, no plantilla). DevTools → Network filtro `askAi` → Status 200.

---

## 🟡 Por hacer · Claude Code (PR #9+ cuando lo pida Carlos)

- **Refactor completo de `index.html` y `app.html`**: eliminar `_KP` (OpenRouter key base64-split), MegaCuaderno + ScanIA a `askAi`, quitar UI de "Config Groq" obsoleta.
- **Refactor `notebook-local.html`**: idéntico patrón que `index.html`.
- **Rename `notebook-groq.html` → `notebook-ia.html`** (ya eliminados `notebook-groq.html` y `notebook-groq-demo.html`, pero revisar si hay links a estos).
- **Strip `api-config.js`** a 0 líneas tras verificar que nadie lo importa.
- **Smoke test en CI**: `test-deploy.sh` debe fallar si encuentra referencias a `api.groq.com`, `openrouter.ai/api`, `pollinations.ai`, `dashscope.aliyuncs.com`, `api.deepseek.com`.

Una vez eso esté en PR y mergeado:
- **Activar App Check enforce** (requiere `window.RECAPTCHA_SITE_KEY` en el frontend y `enforceAppCheck: true` en `askAi.ts`).
- **Firestore rules deny-by-default** (S2.2 del plan original — gran reescritura).

---

## 🛠 Procedimiento post-merge (obligatorio en cada PR)

Tras mergear cualquier PR a `Cartagenaeste/main`, sincronizar el segundo repo:

```bash
cd /Users/carlos/cartagenaestewebappSOLIDA
git checkout main && git pull
TODAY=$(date +%F)
git clone --mirror https://github.com/carlosgalera-a11y/area2cartagena.git /tmp/area2cartagena-backup-$TODAY.git
git push area2 "refs/remotes/area2/main:refs/heads/pre-sync-backup-$TODAY"
git push area2 main:main --force-with-lease
# Esperar a que termine el build:
until [ "$(gh api repos/carlosgalera-a11y/area2cartagena/pages/builds/latest --jq .status)" = "built" ]; do sleep 5; done
curl -sI "https://area2cartagena.es/" | grep -i last-modified
```

Si no se ejecuta este bloque, los usuarios reales siguen viendo la versión anterior.
