# S1.2 · Acciones externas que Carlos ejecuta a mano

Los cambios de código están en la rama `fix/security-cleanup` (commit `f349166`). Este checklist cubre lo que **Claude Code no puede hacer desde el repo** y que queda pendiente de ti.

---

## 🔴 URGENTE · hoy (antes de push)

### 1. Rotar clave DeepSeek
- [ ] Entrar en https://platform.deepseek.com → API Keys
- [ ] **Revocar** la key `sk-6a5ea8dfa7d64c929dad02907917979f` (expuesta en repo público)
- [ ] Generar una nueva
- [ ] Guardarla en Firebase Secrets: `firebase functions:secrets:set DEEPSEEK_KEY`
- [ ] Redeploy Functions: `firebase deploy --only functions`

### 2. Rotar token WP del blog `cartagenaeste.es`
- [ ] Entrar al backend WP → Ajustes → Usuarios/API
- [ ] Revocar el token `[REDACTED_FH_WP_2026-04-21]` (hardcoded en `app-modules.js:156` antes del fix)
- [ ] Si la integración blog→webapp sigue activa, generar uno nuevo y meterlo como secret de Cloud Function (nunca en frontend)
- [ ] Si no se usa, eliminar el código de integración en S4.x

### 3. Verificar usuario admin fantasma en Firebase Auth
- [ ] Firebase Console → Authentication → Users
- [ ] Buscar `admin@cartagena.es`
- [ ] Si existe: **borrarlo**. Era la cuenta de `ADMIN-CREDENTIALS.md`
- [ ] Revisar `users/{uid}` en Firestore por si tenía documento asociado

### 4. Rotar OpenRouter key (recomendación)
- [ ] La clave ofuscada `_dk()` / `_KP` del frontend antiguo también debería rotarse por principio precautorio
- [ ] Entrar en https://openrouter.ai/keys
- [ ] Nueva key → `firebase functions:secrets:set OPENROUTER_KEY`

---

## 🟡 Esta semana

### 5. Push de la rama fix/security-cleanup
- [ ] Revisar el commit `f349166` y el diff
- [ ] `git push -u origin fix/security-cleanup`
- [ ] Abrir PR → revisar → merge a `main`
- [ ] Deploy: `firebase deploy --only hosting,firestore`
- [ ] Validar en https://area2cartagena.es que nada se rompió (lazy-loads IA, login, dashboard)

### 6. Verificar App Check enforce en consola
- [ ] Firebase Console → App Check
- [ ] Firestore → Enforce: **ON**
- [ ] Cloud Functions → Enforce: **ON**
- [ ] Storage → Enforce: **ON**
- [ ] Confirmar que `cartagena-este-webapp` está registrado como app y reCAPTCHA v3 activo

### 7. `git filter-repo` para borrar la clave DeepSeek de la historia

> ⚠️ **Operación destructiva e irreversible** — hay que avisar a cualquier colaborador antes.

```bash
# Solo en local, tras coordinar:
git filter-repo --replace-text <(cat <<'EOF'
sk-6a5ea8dfa7d64c929dad02907917979f==>sk-REDACTED-2026-04-21
[REDACTED_FH_WP_2026-04-21]==>fh_REDACTED-2026-04-21
Cartagena2026!==>REDACTED_PASSWORD
admin@cartagena.es==>removed@example.com
EOF
)

# Tras verificar historia limpia:
git push --force-with-lease origin main
```

Claude Code **no hace esto sin que tú confirmes explícitamente**.

---

## 🟢 Cuando toque (S2+)

### 8. Documentación interna
- [ ] Añadir `docs/security-incident-2026-04-21.md` con: qué se filtró, cuándo se detectó, cuándo se rotó, impact assessment
- [ ] Registrar en tu registro LOPD si aplica (no hubo datos clínicos filtrados, solo credenciales y tokens de servicio)

### 9. Repo privado para el NAS/backend
- [ ] Crear repo privado `Cartagenaeste-proxy-nas`
- [ ] Mover el contenido del antiguo `backend/` (Dockerfile, docker-compose, server.js, package.json) allí
- [ ] El NAS en casa consume ese repo, no el público

### 10. Verificación final
- [ ] Ejecutar `bash test-deploy.sh` → no deben aparecer claves sk- ni IPs 192.168 en HTML
- [ ] Lighthouse en https://area2cartagena.es/ → anotar score antes de S4 para comparar después

---

## Resumen de cambios en esta rama (`fix/security-cleanup`, commit `f349166`)

| Archivo | Acción |
|---|---|
| `ADMIN-CREDENTIALS.md` | Eliminado |
| `sw-v2.js` | Eliminado |
| `login-fix.html` | Eliminado |
| `app-v1773346150.html` | Eliminado |
| `notebook-local (4).html` | Eliminado |
| `backend/` (4 archivos) | Eliminado |
| `index.html` | CSP sin IP; "IA diagnóstica" → "IA docente"; NAS URL opt-in |
| `notebook-local.html` | Igual que index.html |
| `app.html` | Metas SEO: "IA diagnóstica" → "IA docente" |
| `dashboard.html` | NAS URL opt-in; copy "Comprobando NAS" → "Comprobando proxy local" |
| `app-main.js`, `app-modules.js`, `api-config.js` | NAS URL opt-in |
| `test-deploy.sh` | Detección genérica de keys sk-; proxy opcional por env var |
| `firebase.json` | Ignore ampliado; sin referencia a ADMIN-CREDENTIALS.md |
| `ARCHITECTURE.md` | Arquitectura via Cloud Function, no NAS |
| `.gitignore` | Excluye `.claude/` |
| `CLAUDE.md` | Nuevo |
| `docs/diagnostico-2026-04-21.md` | Nuevo |

**22 archivos · 367 inserciones · 6361 eliminaciones** (gran parte son los duplicados borrados).
